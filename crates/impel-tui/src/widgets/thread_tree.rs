//! Thread tree widget

/// Outliner-style thread tree sidebar
pub struct ThreadTree;

impl ThreadTree {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ThreadTree {
    fn default() -> Self {
        Self::new()
    }
}
