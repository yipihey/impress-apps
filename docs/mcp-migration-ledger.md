# MCP migration ledger — TypeScript → Rust

Generated by `scripts/mcp-migration-ledger.py`. Every tool `packages/impress-mcp` exposes, with either the Rust tool that already covers it or a verdict and a reason.

This is the backstop for the retirement: a row that is **deferred** is a decision, not an oversight, and cutover is not done until every **port** row resolves to a real Rust tool.

- TypeScript tools: **229**
- Rust inventory today: **207**
- covered: **148**
- port: **13**
- defer: **28**
- drop: **40**


## bridges (16)

| TypeScript tool | Verdict | Rust equivalent / reason |
|---|---|---|
| `impress_add_papers_from_conversation` | covered | `impress-bridges-service_add-papers-from-conversation` |
| `impress_cite_in_section` | covered | `impress-bridges-service_cite-in-section` |
| `impress_cite_multiple` | covered | `impress-bridges-service_cite-multiple` |
| `impress_cite_paper` | covered | `impress-bridges-service_cite-paper` |
| `impress_conversation_decisions` | covered | `impress-bridges-service_conversation-decisions` |
| `impress_conversation_to_outline` | covered | `impress-bridges-service_conversation-to-outline` |
| `impress_embed_figure` | covered | `impress-bridges-service_embed-figure` |
| `impress_embed_figure_reference` | covered | `impress-bridges-service_embed-figure-reference` |
| `impress_export_conversation_citations` | covered | `impress-bridges-service_export-conversation-citations` |
| `impress_extract_papers_from_conversation` | covered | `impress-bridges-service_extract-papers-from-conversation` |
| `impress_extract_papers_from_text` | covered | `impress-bridges-service_extract-papers-from-text` |
| `impress_get_citation_suggestions` | covered | `impress-bridges-service_get-citation-suggestions` |
| `impress_list_artifacts` | covered | `imbib-artifacts-service_list-artifacts` |
| `impress_list_available_figures` | covered | `impress-bridges-service_list-available-figures` |
| `impress_resolve_artifact` | covered | `impress-bridges-service_resolve-artifact` |
| `impress_sync_figure` | covered | `impress-bridges-service_sync-figure` |

## imbib (85)

| TypeScript tool | Verdict | Rust equivalent / reason |
|---|---|---|
| `imbib_add_annotation` | covered | `imbib-annotations-service_create-annotation` |
| `imbib_add_comment` | covered | `imbib-annotations-service_create-comment` |
| `imbib_add_item_comment` | covered | `imbib-annotations-service_create-comment-on-item` |
| `imbib_add_papers` | covered | `imbib-library-service_import-papers` |
| `imbib_add_tag` | covered | `imbib-tags-service_add-tag` |
| `imbib_add_to_collection` | covered | `imbib-library-service_add-to-collection` |
| `imbib_collection_papers` | covered | `imbib-library-service_list-collection-members` |
| `imbib_compile_manuscript` | covered | `imbib-manuscripts-service_compile-manuscript` |
| `imbib_count_papers` | covered | `imbib-library-service_count-publications` |
| `imbib_create_artifact` | covered | `imbib-artifacts-service_create-artifact` |
| `imbib_create_backup` | covered | `imbib-backup-service_create-backup` |
| `imbib_create_collection` | covered | `imbib-library-service_create-collection` |
| `imbib_create_library` | covered | `imbib-library-service_create-library` |
| `imbib_create_manuscript` | covered | `imbib-manuscripts-service_create-manuscript` |
| `imbib_create_manuscript_from_template` | covered | `imbib-manuscripts-service_create-manuscript-from-template` |
| `imbib_create_smart_search` | covered | `imbib-search-service_create-smart-search` |
| `imbib_create_tag` | covered | `imbib-tags-service_create-tag` |
| `imbib_delete_artifact` | covered | `imbib-artifacts-service_delete-artifact` |
| `imbib_delete_backup` | covered | `imbib-backup-service_delete-backup` |
| `imbib_delete_papers` | covered | `imbib-library-service_delete-publications-undoable` |
| `imbib_delete_tag` | covered | `imbib-tags-service_delete-tag-undoable` |
| `imbib_download_pdfs` | covered | `imbib-app-service_download-pdfs` |
| `imbib_edit_comment` | covered | `imbib-annotations-service_update-comment` |
| `imbib_export_bibtex` | covered | `imbib-library-service_export-bibtex` |
| `imbib_get_artifact` | covered | `imbib-artifacts-service_get-artifact` |
| `imbib_get_library_activity` | covered | `imbib-app-service_recent-activity` |
| `imbib_get_logs` | covered | `imbib-app-service_get-logs` |
| `imbib_get_manuscript` | covered | `imbib-manuscripts-service_get-manuscript` |
| `imbib_get_paper` | covered | `imbib-library-service_get-publication-detail` |
| `imbib_get_smart_search` | covered | `imbib-search-service_get-smart-search` |
| `imbib_inspect_backup` | covered | `imbib-backup-service_inspect-backup` |
| `imbib_link_artifact_to_paper` | covered | `imbib-artifacts-service_link-artifact-to-publication` |
| `imbib_list_annotations` | covered | `imbib-annotations-service_list-annotations` |
| `imbib_list_artifacts` | covered | `imbib-artifacts-service_list-artifacts` |
| `imbib_list_backups` | covered | `imbib-backup-service_list-backups` |
| `imbib_list_collections` | covered | `imbib-library-service_list-collections` |
| `imbib_list_comments` | covered | `imbib-annotations-service_list-comments` |
| `imbib_list_item_comments` | covered | `imbib-annotations-service_list-comments-for-item` |
| `imbib_list_libraries` | covered | `imbib-library-service_list-libraries` |
| `imbib_list_manuscripts` | covered | `imbib-manuscripts-service_list-manuscripts` |
| `imbib_list_smart_searches` | covered | `imbib-search-service_list-smart-searches` |
| `imbib_list_tags` | covered | `imbib-tags-service_list-tags` |
| `imbib_list_templates` | covered | `imbib-manuscripts-service_list-templates` |
| `imbib_mark_read` | covered | `imbib-library-service_set-read` |
| `imbib_prune_backups` | covered | `imbib-backup-service_prune-backups` |
| `imbib_recent_activity` | covered | `imbib-app-service_recent-activity` |
| `imbib_recent_papers` | covered | `imbib-library-service_query-recent` |
| `imbib_recent_undo_groups` | covered | `imbib-undo-service_recent-undo-groups` |
| `imbib_remove_from_collection` | covered | `imbib-library-service_remove-from-collection` |
| `imbib_remove_tag` | covered | `imbib-tags-service_remove-tag` |
| `imbib_rename_tag` | covered | `imbib-tags-service_rename-tag` |
| `imbib_restore_backup` | covered | `imbib-backup-service_restore-backup` |
| `imbib_search_artifacts` | covered | `imbib-artifacts-service_search-artifacts` |
| `imbib_search_library` | covered | `imbib-search-service_full-text-search` |
| `imbib_search_sources` | covered | `imbib-app-service_search-sources` |
| `imbib_set_flag` | covered | `imbib-library-service_set-flag` |
| `imbib_set_tag_color` | covered | `imbib-tags-service_update-tag` |
| `imbib_starred_papers` | covered | `imbib-library-service_query-starred` |
| `imbib_status` | covered | `imbib-app-service_status` |
| `imbib_sync_nudge` | covered | `imbib-app-service_sync-nudge` |
| `imbib_sync_status` | covered | `imbib-app-service_sync-status` |
| `imbib_toggle_star` | covered | `imbib-library-service_set-starred` |
| `imbib_undo` | covered | `imbib-undo-service_undo-batch` |
| `imbib_write_manuscript_body` | covered | `imbib-manuscripts-service_write-manuscript-body` |
| `imbib_create_assignment` | defer | imbib sharing & collaboration — a feature area of its own; port when it is in use. |
| `imbib_delete_assignment` | defer | imbib sharing & collaboration — a feature area of its own; port when it is in use. |
| `imbib_list_assignments` | defer | imbib sharing & collaboration — a feature area of its own; port when it is in use. |
| `imbib_list_paper_assignments` | defer | imbib sharing & collaboration — a feature area of its own; port when it is in use. |
| `imbib_list_participants` | defer | imbib sharing & collaboration — a feature area of its own; port when it is in use. |
| `imbib_list_plot_specs` | defer | Plotting belongs to implore. Porting it into imbib as well is how the drift started. |
| `imbib_render_plot` | defer | Plotting belongs to implore. Porting it into imbib as well is how the drift started. |
| `imbib_save_plot_figure` | defer | Plotting belongs to implore. Porting it into imbib as well is how the drift started. |
| `imbib_save_plot_spec` | defer | Plotting belongs to implore. Porting it into imbib as well is how the drift started. |
| `imbib_set_participant_permission` | defer | imbib sharing & collaboration — a feature area of its own; port when it is in use. |
| `imbib_share_library` | defer | imbib sharing & collaboration — a feature area of its own; port when it is in use. |
| `imbib_unshare_library` | defer | imbib sharing & collaboration — a feature area of its own; port when it is in use. |
| `imbib_add_to_library` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imbib_delete_annotation` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imbib_delete_collection` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imbib_delete_comment` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imbib_delete_smart_searches` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imbib_get_notes` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imbib_resolve_identifier` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imbib_tag_artifact` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imbib_update_notes` | port | No Rust equivalent; needs a new `#[impress_method]`. |

## impart (10)

| TypeScript tool | Verdict | Rust equivalent / reason |
|---|---|---|
| `impart_add_message` | covered | `impart-service_add-message` |
| `impart_branch_conversation` | covered | `impart-service_branch-conversation` |
| `impart_create_conversation` | covered | `impart-service_create-conversation` |
| `impart_get_conversation` | covered | `impart-service_get-conversation` |
| `impart_get_logs` | covered | `impart-service_get-logs` |
| `impart_list_conversations` | covered | `impart-service_list-conversations` |
| `impart_record_artifact` | covered | `impart-service_record-artifact` |
| `impart_record_decision` | covered | `impart-service_record-decision` |
| `impart_status` | covered | `impart-service_status` |
| `impart_update_conversation` | covered | `impart-service_update-conversation` |

## impel (30)

| TypeScript tool | Verdict | Rust equivalent / reason |
|---|---|---|
| `impel_acknowledge_escalation` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_activate_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_block_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_claim_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_complete_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_create_escalation` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_create_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_get_agent` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_get_available_threads` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_get_escalation` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_get_events` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_get_next_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_get_persona` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_get_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_get_thread_events` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_journal_submit` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_kill_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_list_agents` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_list_escalations` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_list_personas` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_list_threads` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_poll_escalation` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_register_agent` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_release_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_resolve_escalation` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_set_temperature` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_status` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_submit_for_review` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_terminate_agent` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |
| `impel_unblock_thread` | drop | impel's own task API re-exposed over MCP. Dropping it means impel is driven from its UI and HTTP API rather than from an MCP client. `impel-core` already holds the task kernel, so an `impel-service` crate would regenerate this whole surface cheaply if it is missed. |

## implore (20)

| TypeScript tool | Verdict | Rust equivalent / reason |
|---|---|---|
| `implore_create_figure` | covered | `implore-service_create-figure` |
| `implore_export_figure` | covered | `implore-service_export-figure` |
| `implore_get_dataset` | covered | `implore-service_get-dataset` |
| `implore_get_figure` | covered | `implore-service_get-figure` |
| `implore_get_logs` | covered | `implore-service_get-logs` |
| `implore_get_status` | covered | `implore-service_status` |
| `implore_list_datasets` | covered | `implore-service_list-datasets` |
| `implore_list_figures` | covered | `implore-service_list-figures` |
| `implore_plot_histogram` | covered | `implore-service_plot-histogram` |
| `implore_plot_series` | covered | `implore-service_plot-series` |
| `implore_rg_batch` | covered | `implore-service_rg-batch` |
| `implore_rg_cascade_plot` | covered | `implore-service_rg-cascade-plot` |
| `implore_rg_colormaps` | covered | `implore-service_rg-colormaps` |
| `implore_rg_control` | covered | `implore-service_rg-control` |
| `implore_rg_load` | covered | `implore-service_rg-load` |
| `implore_rg_slice_png` | covered | `implore-service_rg-slice-png` |
| `implore_rg_slice_raw` | covered | `implore-service_rg-slice-raw` |
| `implore_rg_slice_save` | covered | `implore-service_rg-slice-save` |
| `implore_rg_state` | covered | `implore-service_rg-state` |
| `implore_rg_statistics` | covered | `implore-service_rg-statistics` |

## imprint (55)

| TypeScript tool | Verdict | Rust equivalent / reason |
|---|---|---|
| `imprint_add_citation` | covered | `impress-bridges-service_cite-paper` |
| `imprint_compile` | covered | `imprint-manuscript-service_compile-typst` |
| `imprint_create_comment` | covered | `imprint-app-service_create-comment` |
| `imprint_create_from_template` | covered | `imbib-manuscripts-service_create-manuscript-from-template` |
| `imprint_create_section` | covered | `imprint-manuscript-service_put-section` |
| `imprint_cross_document_search` | covered | `imprint-manuscript-service_search` |
| `imprint_delete_comment` | covered | `imprint-app-service_delete-comment` |
| `imprint_delete_section` | covered | `imprint-manuscript-service_delete-section` |
| `imprint_delete_text` | covered | `imprint-app-service_delete-text` |
| `imprint_export_latex` | covered | `imprint-manuscript-service_export-document` |
| `imprint_export_text` | covered | `imprint-manuscript-service_export-document` |
| `imprint_export_typst` | covered | `imprint-manuscript-service_export-document` |
| `imprint_get_bibliography` | covered | `imprint-app-service_get-bibliography` |
| `imprint_get_citation_usages` | covered | `imprint-manuscript-service_document-citations` |
| `imprint_get_content` | covered | `imprint-app-service_get-content` |
| `imprint_get_document` | covered | `imprint-manuscript-service_get-document` |
| `imprint_get_logs` | covered | `imprint-app-service_get-logs` |
| `imprint_get_manuscript_sections` | covered | `imprint-manuscript-service_list-sections` |
| `imprint_get_outline` | covered | `imprint-manuscript-service_document-outline` |
| `imprint_get_outline_v2` | covered | `imprint-manuscript-service_document-outline` |
| `imprint_get_pdf` | covered | `imprint-app-service_get-pdf` |
| `imprint_get_section` | covered | `imprint-manuscript-service_get-section` |
| `imprint_get_section_body` | covered | `imprint-manuscript-service_get-section` |
| `imprint_insert_citation` | covered | `impress-bridges-service_cite-paper` |
| `imprint_insert_text` | covered | `imprint-app-service_insert-text` |
| `imprint_list_comments` | covered | `imprint-app-service_list-comments` |
| `imprint_list_documents` | covered | `imprint-manuscript-service_list-documents` |
| `imprint_list_manuscripts` | covered | `imbib-manuscripts-service_list-manuscripts` |
| `imprint_list_templates` | covered | `imbib-manuscripts-service_list-templates` |
| `imprint_patch_section` | covered | `imprint-manuscript-service_put-section` |
| `imprint_remove_citation` | covered | `imprint-manuscript-service_replace-in-section` |
| `imprint_replace` | covered | `imprint-app-service_replace` |
| `imprint_search` | covered | `imprint-manuscript-service_search` |
| `imprint_status` | covered | `imprint-app-service_status` |
| `imprint_update_comment` | covered | `imprint-app-service_update-comment` |
| `imprint_accept_suggestion` | defer | imprint AI author-tasks are still moving (Phases 2-4 pending); porting a moving surface invites a second rewrite. |
| `imprint_create_veusz_plot` | defer | Plotting belongs to implore; imprint embeds the result rather than owning it. |
| `imprint_get_operation` | defer | imprint AI author-tasks are still moving (Phases 2-4 pending); porting a moving surface invites a second rewrite. |
| `imprint_insert_veusz_plot` | defer | Plotting belongs to implore; imprint embeds the result rather than owning it. |
| `imprint_list_plot_specs` | defer | Plotting belongs to implore; imprint embeds the result rather than owning it. |
| `imprint_list_tasks` | defer | imprint AI author-tasks are still moving (Phases 2-4 pending); porting a moving surface invites a second rewrite. |
| `imprint_list_veusz_plots` | defer | Plotting belongs to implore; imprint embeds the result rather than owning it. |
| `imprint_open_veusz_plot` | defer | Plotting belongs to implore; imprint embeds the result rather than owning it. |
| `imprint_reject_suggestion` | defer | imprint AI author-tasks are still moving (Phases 2-4 pending); porting a moving surface invites a second rewrite. |
| `imprint_render_plot` | defer | Plotting belongs to implore; imprint embeds the result rather than owning it. |
| `imprint_render_veusz_plot` | defer | Plotting belongs to implore; imprint embeds the result rather than owning it. |
| `imprint_run_task` | defer | imprint AI author-tasks are still moving (Phases 2-4 pending); porting a moving surface invites a second rewrite. |
| `imprint_save_plot_figure` | defer | Plotting belongs to implore; imprint embeds the result rather than owning it. |
| `imprint_save_plot_spec` | defer | Plotting belongs to implore; imprint embeds the result rather than owning it. |
| `imprint_suggest_citations` | defer | imprint AI author-tasks are still moving (Phases 2-4 pending); porting a moving surface invites a second rewrite. |
| `imprint_wait_for_operation` | defer | imprint AI author-tasks are still moving (Phases 2-4 pending); porting a moving surface invites a second rewrite. |
| `imprint_create_document` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imprint_insert_citation_in_section` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imprint_update_document` | port | No Rust equivalent; needs a new `#[impress_method]`. |
| `imprint_update_metadata` | port | No Rust equivalent; needs a new `#[impress_method]`. |

## prompts (10)

| TypeScript tool | Verdict | Rust equivalent / reason |
|---|---|---|
| `focus` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |
| `format` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |
| `name` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |
| `outline` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |
| `page` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |
| `position` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |
| `style` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |
| `title` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |
| `topic` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |
| `what` | drop | MCP prompt templates. This is workflow judgement, which belongs in a skill file rather than an MCP server — retiring them avoids implementing the `prompts/*` protocol in Rust at all. |

## shared-store (3)

| TypeScript tool | Verdict | Rust equivalent / reason |
|---|---|---|
| `impress_get_item` | covered | `impress-bridges-service_get-item` |
| `impress_get_related` | covered | `impress-bridges-service_get-related` |
| `impress_search_all` | covered | `impress-bridges-service_search-all` |
