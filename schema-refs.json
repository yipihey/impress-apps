{
  "$doc": [
    "SINGLE SOURCE OF TRUTH for `items.schema_ref` in the shared impress-core store.",
    "",
    "The store matches `schema_ref` by EXACT EQUALITY (crates/impress-core/src/sql_query.rs).",
    "No version tolerance, no namespace fallback, no fuzzy match. A reader that spells a",
    "ref differently from its writer returns ZERO ROWS forever, silently, on every platform.",
    "That failure mode is indistinguishable from 'the user has no data yet', which is why it",
    "has shipped repeatedly and survived both code review and CI.",
    "",
    "THE RULE: one canonical spelling per record kind, listed once here. Every schema-ref",
    "string literal in the tree (Rust and Swift) must equal one of these. Enforced by",
    "scripts/check-schema-refs.sh (call sites, suite-wide) and crates/*/tests/schema_ref_manifest.rs",
    "(registry parity, both directions, per crate).",
    "",
    "THERE IS NO NAMING CONVENTION TO INFER. Bare (`manuscript`), namespaced (`imbib/library`,",
    "`core/operation`) and versioned (`task@1.0.0`) spellings all exist and all are correct for",
    "their kind. Do not 'regularise' a ref by pattern-matching on its neighbours — that is the",
    "exact reasoning that produced `manuscript-section@1.0.0` and `citation-usage@1.0.0`.",
    "Copy the spelling from this file, not from a sibling call site.",
    "",
    "`knownDivergences` is EMPTY and its budget is 0, as of WP C4. It held three entries:",
    "the task and agent-run spelling splits (three live spellings each) and the dead",
    "`impress/operation` registration. All three are resolved — the code by aligning every",
    "writer, reader and registry, and the DATA by",
    "`impress_core::task_schema_migration` (flagged off, dry-run first, reversible via a",
    "changed-id ledger in `store_metadata`). The budget is a RATCHET: it may only shrink.",
    "At 0, adding a divergence means raising the budget in the same change, in writing,",
    "with the owner decision that made a second spelling of one kind acceptable."
  ],
  "canonical": {
    "ai-import-ledger@1.0.0": "impress-core AI schemas. One atomic, content-hashed receipt per imported legacy LocalModels conversation; written by impress-ai so migration is restart-safe and changed source rows are surfaced instead of silently duplicated.",
    "agent-run@1.0.0": "VERSIONED, and the ONLY spelling of this kind since WP C4. Written by impel-core task_store AGENT_RUN_SCHEMA and by impel SharedTaskBridge.agentRoundCompleted; registered ONCE, in impress-core schemas/task.rs (impel-core's register_impel_schemas delegates there); claimed by the AgentRunRecordKind descriptor, which is what AgentStoreReader and the agentRunsAll sidebar count read. The retired `agent-run` (impress-core registration, unwritten) and `impel/agent-run` (SharedTaskBridge) rows are converged by impress_core::task_schema_migration.",
    "annotation": "impress-core. Written by imbib annotation sync.",
    "bibliography-entry": "impress-core registry only — NOTHING writes the bare form; THE publication ref is `imbib/bibliography-entry` (reading this one is the iOS-citation-picker bug). Kept in `canonical` rather than moved to `registeredButUnwritten` because reclassifying it would reject existing call sites and belongs to the imbib pass, not C4's. This note used to say 'see registeredButUnwritten', which held only `impress/operation` and is now empty.",
    "chat-message": "impress-core + impart-core. Written by impart ImpartStoreAdapter.",
    "citation-usage": "impress-core. Written BARE by imprint ImprintStoreAdapter.recordCitationUsage. Read by imprint ImprintImpressStore + imbib CitationUsageReader.",
    "collection": "impress-core generic collection kernel (ADR-0022 D1).",
    "content-blob@1.0.0": "impress-core AI schemas. Immutable content-addressed metadata for multimodal bytes; written by impress-ai. Bytes live behind the BlobStore port, never inline in payload_json.",
    "content-chunk@1.0.0": "impress-core source schemas. Domain-neutral text derived from immutable source bytes, pinned to an extraction run and structured source locator.",
    "conversation@1.0.0": "impress-core AI schemas. Durable research conversation parent; written and read by impress-ai and intended for impart macOS/iOS presentation.",
    "core/operation": "The operation journal / CRDT log. Written and queried by impress-core sqlite_store; its one schema definition lives in imbib-core unified::schemas::core_operation_schema. NOT `impress/operation`: impress-core registered that id from the start and nothing ever wrote or read it — the dead registration (schemas/operation.rs) was deleted in WP C4. Remaining wart, deliberately not fixed here: the WRITER crate (impress-core) registers no definition for this id, so registry.validate() is a no-op for operation rows in an impress-core-only registry. Moving the definition up from imbib-core is the fix and is out of C4's boundary.",
    "dataset": "impress-core (implore schemas). Written by implore ImploreStoreAdapter.",
    "email-message": "impress-core + impart-core. Written by impart.",
    "extraction-run@1.0.0": "impress-core source schemas. Reproducibility record for text, OCR, layout, table, or figure extraction; written by domain ingestion adapters.",
    "figure": "impress-core (implore schemas). Written by implore ImploreStoreAdapter.",
    "figure-collection": "impress-core (implore schemas). Figure folder envelope.",
    "git-project": "impress-core.",
    "imbib/activity-record": "imbib-core.",
    "imbib/annotation": "imbib-core.",
    "imbib/assignment": "imbib-core.",
    "imbib/bibliography-entry": "imbib-core. THE publication ref. Written by imbib-core conversion.rs:113. Reading bare `bibliography-entry` is the iOS-citation-picker bug. Also the impel enrichment spawn trigger (impel-enrichment BIBLIOGRAPHY_ENTRY_SCHEMA, scanned by impel-taskd): that constant read `bibliography-entry@1.0.0` until 2026-07-29, so the pipeline matched zero rows and never spawned a task — fixed, and pinned against this writer by `enrichment_trigger_matches_the_ref_imbib_actually_writes`.",
    "imbib/collection": "imbib-core.",
    "imbib/comment": "imbib-core.",
    "imbib/dismissed-paper": "imbib-core.",
    "imbib/library": "imbib-core.",
    "imbib/linked-file": "imbib-core.",
    "imbib/muted-item": "imbib-core.",
    "imbib/recommendation-profile": "imbib-core.",
    "imbib/scix-library": "imbib-core.",
    "imbib/smart-search": "imbib-core.",
    "imbib/tag-definition": "imbib-core.",
    "impress/artifact/code": "impress-core artifact domain.",
    "impress/artifact/dataset": "impress-core artifact domain.",
    "impress/artifact/general": "impress-core artifact domain.",
    "impress/artifact/media": "impress-core artifact domain.",
    "impress/artifact/note": "impress-core artifact domain.",
    "impress/artifact/poster": "impress-core artifact domain.",
    "impress/artifact/presentation": "impress-core artifact domain.",
    "impress/artifact/webpage": "impress-core artifact domain.",
    "mail-account": "impress-core. Written by impart ImpartStoreAdapter, and (fixtures only) by impress-iOS ImpressIOSUITestSeed, which takes the spelling from here rather than from a sibling call site.",
    "mail-folder": "impress-core. Written by impart ImpartStoreAdapter, and (fixtures only) by impress-iOS ImpressIOSUITestSeed.",
    "manuscript": "impress-core + imbib-core. Written by imprint ManuscriptStoreAdapter, impel JournalPipeline. ADR-0023 W3 added the OPTIONAL payload field `external_source` (JSON: path + content_hash + state present|missing + watched-file/folder refs). Present ⇒ the row is a REFERENCE-IN-PLACE index entry: the FILE is authoritative for the body, `body_content` is a snapshot replaced on every re-read, the row takes no editor session, and nothing writes the file back (D4). Written by imprint ManuscriptStoreAdapter.upsertExternalManuscript. Do NOT confuse with `import_source`, which asserts the opposite (a copy was taken; the original is detached). The schema ref itself is unchanged and kind_scope stays `manuscript`.",
    "manuscript-collection": "impress-core + imbib-core. Manuscript folder envelope; written by imprint ManuscriptMigrationRunner.",
    "manuscript-revision": "impress-core + imbib-core. Written by impel JournalSnapshotJob.",
    "manuscript-section": "impress-core + imbib-core. Written BARE by imprint ImprintStoreAdapter.syncSections (Swift, gated on hasThroughline) and crates/imprint-service/src/sections.rs SECTION_SCHEMA_REF (Rust, app-closed/MCP path). Reading `manuscript-section@1.0.0` was the outline/sections/cross-doc-search bug.",
    "manuscript-submission": "impress-core. Written by impel JournalSubmission.",
    "plot-spec": "impress-core.",
    "review": "impress-core knowledge objects. Written by impel JournalReviewService.",
    "review-request@1.0.0": "VERSIONED, and in NO registry. Written by impel-core task_store REVIEW_REQUEST_SCHEMA, validated by impress-store-ffi resolve_review, read by imprint ThroughlineCoordinator. Canonical because it is genuinely the written spelling; the missing registration is tracked in registeredButUnwritten's inverse and is why registry.validate() is a no-op for these rows.",
    "revision-note": "impress-core knowledge objects. Written by impel JournalReviewService.",
    "source-citation@1.0.0": "impress-core source schemas. Immutable-byte-pinned structured citation with page, region, range, section, figure, and table location; written by curation and ingestion adapters.",
    "task@1.0.0": "VERSIONED, and the ONLY spelling of this kind since WP C4. Selected by impress-core sqlite_store ready_tasks — which is why it won: it is the spelling with a live queue behind it, so converging on it means nothing that is scheduled today stops being scheduled. Written by impel-core task_spawn TASK_SCHEMA and by impel SharedTaskBridge.taskCreated; registered ONCE, in impress-core schemas/task.rs, whose fields are the union of both writers' payloads; inherited by manuscript-submission; claimed by the TaskRecordKind descriptor, which is what AgentStoreReader and the agentTasksAll sidebar counts read. The retired `task` (impress-core registration, unwritten in production) and `impel/task` (SharedTaskBridge) rows are converged by impress_core::task_schema_migration. NOTE: ready_tasks additionally requires a non-empty payload `task_kind`, so bridge-MIRRORED rows converge without becoming schedulable.",
    "figure-region@1.0.0": "VERSIONED. PDF figure/image region provenance for vw knowledge ingest (9aec0265). Registered in impress-core schemas/source.rs (FIGURE_REGION_SCHEMA); written AND read exclusively through impress-store-service source_service.rs via that same constant, so writer and readers cannot drift. Added to the manifest one commit late — the schema-refs pre-push gate caught the omission.",
    "tool-invocation@1.0.0": "impress-core AI schemas. One attributed SciX, Impress MCP, web, or other tool call made during an agent/model run; written by impress-ai.",
    "throughline": "impress-core (ADR-0016). Written by imprint ImprintStoreAdapter.",
    "veusz-plot": "impress-core.",
    "vw/command-receipt@1.0.0": "vw-impress-adapter. Durable idempotency receipt keyed from a caller command UUID and containing the original typed session result.",
    "vw/configuration@1.0.0": "vw-impress-adapter. Exact vehicle configuration and deviations used for knowledge applicability.",
    "vw/diagnostic-session@1.0.0": "vw-impress-adapter. Persistent VW diagnostic aggregate pinned to a knowledge-pack and deterministic-engine version.",
    "vw/knowledge-pack@1.0.0": "vw-impress-adapter. Immutable curated VW knowledge-pack manifest and typed content.",
    "vw/measurement@1.0.0": "vw-impress-adapter. Queryable child measurement with unit, acquisition context, and parent diagnostic session.",
    "vw/observation@1.0.0": "vw-impress-adapter. Queryable child observation with acquisition context and supersession history.",
    "vw/procedure-run@1.0.0": "vw-impress-adapter. Durable execution state for a published diagnostic procedure.",
    "vw/vehicle@1.0.0": "vw-impress-adapter. Vehicle identity linked to its exact configuration.",
    "watched-file@1.0.0": "VERSIONED. impress-core schemas/watched_folder.rs (ADR-0023 D4). One row per file a watched folder has ever discovered — an INDEX ENTRY, never a copy: path, sha256 content hash, mtime, and a `state` of present|missing. Written and read ONLY by impress-store-service DocsImportService (import_discovered / finish_watched_scan / record_produced_rows / list_watched_files), which is the single writer for both ingest units. A vanished file's row is marked `missing`, never deleted, because the rows it produced are still real. `produced_ids` is the W2 seam: discovery leaves it absent and the app's real importer fills it in through record_produced_rows.",
    "watched-folder@1.0.0": "VERSIONED, exactly as ADR-0023 D2 names it. impress-core schemas/watched_folder.rs. One row per folder the user (or an agent, via MCP/CLI) asked the suite to watch: path + optional security-scoped bookmark, kind_scope, enabled, declared volume_state (D6) and last-scan stats. Written by impress-store-service DocsImportService add_watched_folder / update_watched_folder / finish_watched_scan; W1's Swift FolderWatchService reaches it through those verbs, never with its own writer. The watched file TYPES and the ingest UNIT are deliberately NOT fields here — they are derived from the record kind's FileDiscoveryCapability (D1), which is the one authority for both."
  },
  "$registriesDoc": [
    "Exactly what each crate's schema-registration entry point produces. Asserted in BOTH",
    "directions by crates/<crate>/tests/schema_ref_manifest.rs — a new, renamed or dropped",
    "schema fails its crate's test until this list is updated in the same PR. Per-crate so",
    "no new dependency edges are needed to make each direction total.",
    "",
    "Entry points: impress-core schemas::register_core_schemas, imbib-core",
    "unified::schemas::register_all, impel-core schemas::register_impel_schemas,",
    "impart-core schemas::register_impart_schemas, and vw-impress-adapter",
    "schemas::register_vw_schemas.",
    "",
    "The same id may appear under two crates, but there is only ever ONE DEFINITION of it.",
    "`task@1.0.0` / `agent-run@1.0.0` are defined in impress-core (schemas/task.rs, next to",
    "the `ready_tasks` query that consumes them) and impel-core's entry point DELEGATES",
    "there — it does not keep a copy. Two definitions of one id drift, which is exactly how",
    "impel ended up with a registry saying `impel/task` while its kernel wrote `task@1.0.0`."
  ],
  "registries": {
    "impress-core": [
      "ai-import-ledger@1.0.0",
      "agent-run@1.0.0",
      "annotation",
      "bibliography-entry",
      "chat-message",
      "citation-usage",
      "collection",
      "content-blob@1.0.0",
      "content-chunk@1.0.0",
      "conversation@1.0.0",
      "dataset",
      "email-message",
      "extraction-run@1.0.0",
      "figure",
      "figure-collection",
      "figure-region@1.0.0",
      "git-project",
      "impress/artifact/code",
      "impress/artifact/dataset",
      "impress/artifact/general",
      "impress/artifact/media",
      "impress/artifact/note",
      "impress/artifact/poster",
      "impress/artifact/presentation",
      "impress/artifact/webpage",
      "mail-account",
      "mail-folder",
      "manuscript",
      "manuscript-collection",
      "manuscript-revision",
      "manuscript-section",
      "manuscript-submission",
      "plot-spec",
      "review",
      "revision-note",
      "source-citation@1.0.0",
      "task@1.0.0",
      "tool-invocation@1.0.0",
      "throughline",
      "veusz-plot",
      "watched-file@1.0.0",
      "watched-folder@1.0.0"
    ],
    "vw-impress-adapter": [
      "vw/command-receipt@1.0.0",
      "vw/configuration@1.0.0",
      "vw/diagnostic-session@1.0.0",
      "vw/knowledge-pack@1.0.0",
      "vw/measurement@1.0.0",
      "vw/observation@1.0.0",
      "vw/procedure-run@1.0.0",
      "vw/vehicle@1.0.0"
    ],
    "imbib-core": [
      "core/operation",
      "imbib/activity-record",
      "imbib/annotation",
      "imbib/assignment",
      "imbib/bibliography-entry",
      "imbib/collection",
      "imbib/comment",
      "imbib/dismissed-paper",
      "imbib/library",
      "imbib/linked-file",
      "imbib/muted-item",
      "imbib/recommendation-profile",
      "imbib/scix-library",
      "imbib/smart-search",
      "imbib/tag-definition",
      "impress/artifact/code",
      "impress/artifact/dataset",
      "impress/artifact/general",
      "impress/artifact/media",
      "impress/artifact/note",
      "impress/artifact/poster",
      "impress/artifact/presentation",
      "impress/artifact/webpage",
      "manuscript",
      "manuscript-collection",
      "manuscript-revision",
      "manuscript-section"
    ],
    "impel-core": [
      "agent-run@1.0.0",
      "task@1.0.0"
    ],
    "impart-core": [
      "chat-message",
      "email-message"
    ]
  },
  "registeredButUnwritten": {},
  "literalAllowlist": {
    "not-a-manuscript-section": "Negative fixture — ImprintImpressStoreTests asserts loadSection REJECTS a wrong-schema row.",
    "s": "Opaque placeholder in SyncEngineCodecTests round-trip; the codec never interprets it.",
    "test": "impress-core sqlite_store unit-test fixture schema.",
    "nothing": "impress-store-ffi fixture asserting count_by_schema returns 0 for an unknown schema.",
    "preprint": "impress-core registry unit-test fixture for validate-unknown-schema.",
    "publication@1.0.0": "impress-core backup.rs unit-test fixture; the backup format stores whatever ref a row carries, so the value is arbitrary here.",
    "manuscript-bundle-manifest@1.0.0": "NOT a store schema_ref. This is the self-describing `schema` field INSIDE a bundle-manifest JSON payload (impress-core schemas/manuscript_bundle_manifest.rs), which is versioned by design. No row is ever written with this ref.",
    "manuscript-bundle-manifest@2.0.0": "Same as above — a CounselEngine test asserting an unknown manifest payload version is rejected.",
    "impress/litmus-note": "Test-only record kind (LitmusNoteRecordKind, PMC tests): registered from TEST code to prove ADR-0021's add-a-kind-with-zero-chassis-edits litmus continuously in CI. Never written to any store; no registry entry by design."
  },
  "knownDivergences": [],
  "knownDivergenceBudget": 0
}
